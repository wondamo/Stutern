{
  "mcpServers": {
    "postgres-fx": {
      "command": "python",
      "args": ["-m", "mcp_postgres_server.server"],
      "env": {
        "TRANSPORT_PROTOCOL": "stdio",
        "DATABASE_URL": "postgres://app:app@localhost:5432/stutern"
      }
    }
  }
}
