# Use either DATABASE_URL or discrete PG* vars.
# DATABASE_URL takes precedence when set.

# DATABASE_URL=postgres://user:password@localhost:5432/mydb
# TRANSPORT_PROTOCOL=stdio

# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=secret
# PGDATABASE=mydb

# Enable SSL unless 'disable'. If enabled, certificate validation is turned off for convenience.
# PGSSLMODE=require

